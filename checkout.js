const foo = 'bar';

const bar = 'foo';

const foobar = () => {
  console.log(foo + bar);
};

const firstname = 'John';
const lastname = 'Doe';

const meinname = () => {
  return firstname + lastname;
};
